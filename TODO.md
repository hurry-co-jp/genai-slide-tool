# 今後のロードマップ

## 1. main.js のリファクタリング
現在の `main.js` は700行を超え、様々な責任が混在しています。関心を分離します。

- [ ] **`utils.js` の抽出**
    - ヘルパー関数（例：`el()`, `copyToClipboard`, `downloadFile`）。
- [ ] **`ui-components.js` の抽出**
    - モーダルプレビューのロジック。
    - 生成設定（ポップオーバー/スライダー）のロジック。
    - 行番号レンダリングのロジック。
- [ ] **`handlers.js` の抽出**
    - アプリケーションロジックとイベントハンドラ（例：`handleGenerateDesign`, `handleRenderMarp`）。

## 2. 計画モード（評価＆チャット）の実装
原稿の品質を評価するために、「ドラフト」の前に新しいワークフローフェーズ「計画」を追加します。

- [ ] **計画モード用の3カラムレイアウトの作成**
    - **列 1:** 原稿エディタ（ドラフトモードと共有）。
    - **列 2:** **評価レポート**（新コンポーネント）。
        - AIが原稿の完全性、論理性、構造を分析します。
    - **列 3:** **AIチャットアシスタント**（新コンポーネント）。
        - 評価に基づいて特定のセクションを改善するための対話型チャット。
